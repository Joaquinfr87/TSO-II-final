# /etc/dovecot/conf.d/10-logging.conf — Logging en Dovecot (Docker)

# Log a archivo en vez de syslog
log_path = /var/log/dovecot.log

# Información útil para debugging
auth_verbose = yes
auth_verbose_passwords = no
auth_debug = no
mail_debug = no